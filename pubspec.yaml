name: devlog
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.1.3 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  logs_api:
    path: packages/logs_api
  local_storage_logs_api:
    path: packages/local_storage_logs_api
  logs_repository:
    path: packages/logs_repository
  shared_preferences: ^2.2.2
  bloc: ^8.1.2
  flutter_bloc: ^8.1.3
dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
